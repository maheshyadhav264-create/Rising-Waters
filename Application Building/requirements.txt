Flask>=3.0
joblib>=1.3
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24

# Optional, only needed to re-run model_training/train_model.py:
matplotlib>=3.7

# Optional: if you have xgboost available, you can swap it in for
# GradientBoostingClassifier in model_training/train_model.py.
# xgboost>=2.0
